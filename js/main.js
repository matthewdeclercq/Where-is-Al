// Main page functionality for Where Is Al
(function() {
    'use strict';

    // Map auto-refresh functionality
    // Uncomment and configure when Garmin map is embedded
    /*
    function refreshMap() {
        const mapIframe = document.querySelector('.map-container iframe');
        if (mapIframe) {
            const src = mapIframe.src;
            mapIframe.src = '';
            setTimeout(() => {
                mapIframe.src = src;
            }, 100);
        }
    }
    // setInterval(refreshMap, 300000); // Refresh every 5 minutes
    */
})();

